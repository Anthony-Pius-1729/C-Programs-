{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "cppbuild",
      "label": "C/C++: gcc.exe build active file",
      "command": "F:\\msys\\mingw64\\bin\\gcc.exe", // Use GCC from your specified path
      "args": [
        "-fdiagnostics-color=always", // Enable colored diagnostics
        "-g", // Include debugging information
        "${file}", // Compile the active file
        "-o", // Output flag
        "${fileDirname}\\${fileBasenameNoExtension}.exe" // Output file name
      ],
      "options": {
        "cwd": "F:\\msys\\mingw64\\bin" // Set current working directory
      },
      "problemMatcher": [
        "$gcc" // Use GCC's error format
      ],
      "group": {
        "kind": "build", // Group this task as a build
        "isDefault": true // Mark this as the default build task
      },
      "detail": "Task generated by Debugger." // Optional detail for debugging
    }
  ]
}
